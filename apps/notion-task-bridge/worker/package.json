{
	"name": "@orbytes/notion-task-bridge-worker",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"d1:apply": "wrangler d1 migrations apply notion-task-bridge --local",
		"d1:apply:remote": "wrangler d1 migrations apply notion-task-bridge --remote",
		"generate:toolkit": "node scripts/generate-toolkit-context.mjs"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.14.1",
		"@cloudflare/workers-types": "^4.20260404.1",
		"typescript": "^5.5.2",
		"vitest": "^4.1.0",
		"wrangler": "^4.80.0"
	}
}
